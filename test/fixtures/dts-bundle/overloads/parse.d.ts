export declare function parse(input: string): object;
export declare function parse(input: Buffer): object;
export declare function parse(input: string | Buffer, options: {
    strict: boolean;
}): object;
